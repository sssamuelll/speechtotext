# El contrato — plan 2c de 4 del lanzamiento público

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar las superficies que le faltan a la librería antes de publicarla: el contrato público documentado entero y vigilado por un test que se rompe con ruido, fixtures sin habla humana real ni rutas de máquina, CI en tres sistemas con un job que empaqueta e instala como lo haría un usuario, y el servidor MCP.

**Architecture:** Nada de esto añade capas. `docs/api.md` gana la sección que le falta (`audio/` entero) y `tests/test_api_contract.py` la vigila en dos direcciones sin parsear firmas: lo que un `__all__` exporta tiene que estar nombrado en el documento, y lo que una lista curada declara contrato tiene que importar y estar nombrado. El MCP es una capa fina sobre `core.transcribe`, `core.finder`, `speakers.registry` y `core.probe`: cuatro funciones planas con anotaciones — probables sin el SDK — y un `serve()` que es lo único que lo importa. El CI pasa de dos combinaciones rotas a seis que corren más un job que construye el wheel y lo instala en un venv limpio.

**Tech Stack:** Python ≥ 3.11, pytest, Typer, GitHub Actions, SDK oficial `mcp` v2 (extra opcional).

**Spec:** `docs/superpowers/specs/2026-09-12-lanzamiento-publico-design.md` — §6.2 (MCP), §6.3 (`docs/api.md`), §6.4 (`tests/test_api_contract.py`), §7 (pruebas y CI).

**Base:** `main` en `b637c71` (plan 2b mergeado, PR #34). Rama: `feat/contrato`. Suite hoy: **435 tests verdes**.

## Global Constraints

Toda tarea hereda esto. Los valores son literales, copiados del spec o verificados contra el árbol.

- **Gate**, desde la raíz del repo: `.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider`. Verde antes de cada commit.
- **TDD**: test que falla primero, implementación mínima después, gate, commit. Un commit por tarea.
- **Commits en español**, `tipo(ámbito): qué`. Sin prefijos en inglés.
- **Nombres prohibidos.** `git grep -iE 'aurelius|jarvis|klara'` sobre `src tests scripts README.md CHANGELOG.md docs/api.md` tiene que salir **vacío** y seguir vacío. Hoy lo está.
- **Nada de personas, medios ni ciudades reales** en el árbol público: ni en fixtures, ni en tests, ni en ejemplos de documentación.
- **Nada de rutas de máquina** (`C:\Users\<quien sea>`, `D:\...`) en archivos versionados. Los ejemplos usan rutas genéricas.
- **La suite no toca red, ni GPU, ni descarga modelos.** `tests/conftest.py` fija la máquina sondeada y un `SPEECHTOTEXT_HOME` temporal para cada test; lo que necesita red va con doble (`urlopen`, `subprocess`, `sys.modules`).
- **Piso de Python: 3.11** (`requires-python = ">=3.11"`). Nada de sintaxis de 3.12+.
- **Lo que cambie en `docs/api.md` sale en `CHANGELOG.md`**, bajo *rompe* si rompe. Regla existente del repo.
- **Dependencias nuevas: solo el extra opcional `[mcp]`.** El núcleo no gana ninguna.
- **El núcleo nunca imprime.** Vale doble para el servidor MCP: escribir en stdout rompe el protocolo stdio.

---

## Decisiones ya tomadas (no re-litigar)

Escribí el plan después de medir el árbol. Estas quedaron cerradas; el ejecutor las aplica, no las discute.

**R1 — `audio/` se documenta entero, no se recorta.** El grep dice que, fuera de `audio/`, el único símbolo con consumidor en este repo es `decode_audio` (lo usa `core/transcribe.py:load_audio`); los otros 19 solo los usan sus propios tests. El instinto de cortarlos es correcto en general y **equivocado aquí**: el spec §3 lo decidió explícito — *"`audio/` se queda entero … `AudioClip`/`AudioView`/proveniencia siguen siendo la entrada por clip para la persona B"* — y `compute_voice_evidence` ya es contrato de un consumidor real. Se documenta. Costo si me equivoco: unas 120 líneas de `api.md` describiendo tipos que nadie importa todavía.

**R2 — Son 23 nombres, no 16.** El spec decía "los 18 símbolos de `audio.__all__` (hoy 2 documentados)". Contado contra el árbol de hoy: `audio.__all__` tiene **20** (el spec contó antes de que `gate.py` añadiera sus cuatro por `__all__ +=`) y **17** no están nombrados en `api.md`. `asr.__all__` tiene 10 y le faltan **3** (`Cap`, `TranscriptionSegment`, `TranscriptionWord`). Y **3** rutas de la lista curada no están nombradas (`EngineInfo`, `DiarizationReport`, `assign_names`). Total: **23**.

**R3 — El test de contrato no parsea firmas.** El spec pedía que "cada símbolo que `api.md` nombra en un bloque de firma tiene que existir e importarse". Un parser de firmas Markdown se pudre más rápido que lo que vigila. En su lugar, dos direcciones sin parser: **(a)** mecánica — cada nombre de `asr.__all__` y `audio.__all__` tiene que aparecer como identificador dentro de un bloque de código, un *code span* o un encabezado de `api.md`; **(b)** curada — una lista explícita de rutas punteadas escrita en el propio test, cada una tiene que importar y aparecer. La (a) caza el modo de fallo real (exportar algo y no documentarlo); la (b) caza el inverso (documentar lo que ya no existe). Costo si me equivoco: borrar un símbolo de `api.md` sin quitarlo de la lista no se detecta — pero eso no es el modo de fallo que hizo falta un test.

**R4 — Sin marcador `@pytest.mark.network`.** El spec lo pedía "para los tests que lo necesitan". Hoy no hay ninguno: el único `urlopen` de la suite está stubbeado (`tests/test_enginepin.py:116`). Un marcador para cero tests es ceremonia. Cuando aparezca el primero, se añade entonces.

**R5 — Matriz de CI: 3 sistemas × Python 3.11 y 3.14**, no 3.13 como decía el spec. 3.11 es el piso que declara `requires-python` y que nadie prueba en local; 3.14 es lo que corre la máquina de desarrollo (los `.pyc` del árbol son `cpython-314`). 3.13 no verifica ni el piso ni el entorno real: verifica el medio, que es lo que se interpola solo. Costo si me equivoco: si alguna rueda (`av`, `numpy`) todavía no existe para 3.14 en macOS, ese job cae — se baja **ese par** a 3.13 y se anota en el comentario del workflow. No se baja la matriz entera.

**R6 — La fixture sintética sigue siendo un archivo.** Dos tests la leen de `tests/fixtures/` y uno la copia con `shutil.copyfile` desde el stub de `subprocess.run`. Generarla en cada test añadiría una fábrica para un consumidor y medio. Lo que cambia es el **contenido**: texto sintético, `params.model` sin ruta de máquina.

**R7 — El MCP se parte en dos.** Cuatro funciones planas con anotaciones que no tocan el SDK, y un `serve()` que lo importa perezosamente y las registra. El SDK no está en `[dev]`, así que los tests prueban las cuatro funciones sin él; el registro se cubre sembrando un doble de `mcp.server` en `sys.modules`, el mismo patrón que `tests/test_models.py` ya usa con `huggingface_hub`.

**R8 — `mcp>=2.2,<3`, API v2.** `from mcp.server import MCPServer`, `@mcp.tool()` (acepta funciones síncronas), `servidor.run()` (stdio por defecto). Verificado contra `docs_src/index/tutorial001.py` del SDK y contra la firma `def run(self, transport: Literal["stdio", "sse", "streamable-http"] = "stdio") -> None`. La v1 usaba `mcp.server.fastmcp.FastMCP`: **no es esta**.

**R9 — La función se llama `list_voices`, no `voices`.** El spec §6.3 la nombró mal. Se documenta y se usa `registry.list_voices`.

---

## Callers verificados

Todo lo que este plan toca, con quién lo usa hoy. Medido con `git grep`, no recordado.

| Qué | Quién lo usa hoy | Qué implica |
|---|---|---|
| `tests/fixtures/whispercpp_ojf.json` | `tests/test_whispercpp_backend.py:19` (constante `FIXTURE`; `_run_stub` la copia como salida del subprocess) y `tests/test_formats_speaker.py:248` | La Tarea 3 toca esos dos archivos y ningún otro. |
| Asserts sobre el **contenido** de la fixture | solo `test_transcribe_parsea_la_fixture_real` y `test_pipeline_whispercpp_fixture_a_write_json` | Los demás tests del archivo la usan como "salida válida cualquiera" y no miran el texto. |
| `audio.__all__` (20 nombres) | fuera de `audio/`: **solo** `decode_audio`, en `core/transcribe.py:load_audio` | Ver R1: se documenta entero igual. |
| `asr.__all__` (10 nombres) | `core/transcribe.py`, `cli/app.py`, `core/benchmark.py`, los dos backends | — |
| `.github/workflows/tests.yml` | nadie lo ejecuta con éxito: instala `".[dev,evaluation]"` y el extra `evaluation` se fue en `59730b8` ("el corte") | La Tarea 4 lo arregla. Es la razón por la que el badge de CI no se puede poner todavía. |
| `src/static/audio.wav` | solo el docstring de cabecera de `cli/app.py:7` | La Tarea 6 cambia el ejemplo; el wav se purga de la historia en §9.5. |
| `registry.list_voices` | `cli/app.py:568` (comando `voices`) | La Tarea 5 la reusa tal cual. |
| `finder.load_or_build_index(audio, scan_model, rebuild=False) -> (segments, cached)` y `finder.search(segments, query, gap=60.0, top=5) -> list[Region]` | `cli/app.py:501-505` | La Tarea 5 los reusa tal cual. `Region(start, end, hits, matches, snippet)`. |
| `core.formats.write_json(segments, info, path, *, engine_info=None, speech_s=None, gaps=None)` | `cli/app.py:326`, `tests/test_formats_speaker.py` | La Tarea 5 lo llama con la misma forma de `info` que el CLI (`SimpleNamespace(language, language_probability, duration)`). |
| `probe.machine()` / `probe.choose_route(m, model, ...)` | `cli/app.py:773`, `core/transcribe.py:resolve_route` | La Tarea 5 los reusa tal cual. |
| Comentario de `.gitignore` (`src/speechtotext/models/ es un paquete Python versionado`) | ya no es cierto: ese paquete se fue en `59730b8` | La Tarea 6 corrige el comentario; el ancla `/models/` se queda. |
| `tests/conftest.py` (`_hermetico`, autouse) | toda la suite | Fija `SPEECHTOTEXT_HOME` y `probe.machine`. Las Tareas 2 y 5 cuentan con él; ninguna lo modifica. |

---

## File Structure

**Crear:**

| Archivo | Responsabilidad |
|---|---|
| `tests/test_api_contract.py` | El mapa se rompe con ruido: `__all__` ⊆ `api.md`, y lista curada ⊆ (importa ∩ `api.md`). Sin parser de firmas. |
| `src/speechtotext/cli/mcp_server.py` | Cuatro funciones planas (`transcribe`, `find`, `voices`, `probe`) + `serve()`. El SDK solo se importa dentro de `serve()`. |
| `tests/test_mcp.py` | Las cuatro funciones sin SDK; `serve()` con un doble de `mcp.server` en `sys.modules`. |

**Modificar:**

| Archivo | Qué cambia |
|---|---|
| `docs/api.md` | Sección nueva `## Capa audio/`; tres tipos en la sección `asr/`; `EngineInfo`/`DiarizationReport` nombrados en `transcribe()`; `assign_names` nombrado en `Identificación`; párrafo del MCP. |
| `tests/fixtures/whispercpp_ojf.json` | Contenido sintético con la misma forma. |
| `tests/test_whispercpp_backend.py`, `tests/test_formats_speaker.py` | Asserts contra el contenido nuevo. |
| `.github/workflows/tests.yml` | Extra roto, matriz de 6, job `package`. |
| `pyproject.toml` | Extra `[mcp]`. |
| `src/speechtotext/cli/app.py` | Comando `mcp`; docstring de cabecera sin `src/static/audio.wav`. |
| `README.md`, `CHANGELOG.md`, `.gitignore` | Documentación y limpieza final. |

---

## Orden y por qué

1. **api.md** (doc sola) — 2. **el test que la vigila** — 3. **la fixture** — 4. **el CI** — 5. **el MCP** — 6. **README/CHANGELOG y barridos**.

Las tareas 1 y 2 van en ese orden a propósito: el test se escribe contra un documento ya completo, y su Paso 2 demuestra que muerde metiendo un nombre falso en `__all__`. Escribirlo antes dejaría la Tarea 1 en rojo, que es peor que invertir el ciclo. Las tareas 3, 4 y 5 son independientes entre sí; la 6 va al final porque documenta lo que la 5 añadió.

---

### Task 1: `docs/api.md` — la capa `audio/` y los tipos de `asr/` que faltan

**Files:**
- Modify: `docs/api.md`

**Interfaces:**
- Consumes: nada.
- Produces: los 23 nombres que la Tarea 2 va a exigir. La Tarea 2 falla si esta tarea deja alguno fuera.

Tarea de documentación pura: no se toca una línea de código. La verificación es un conteo mecánico (Paso 3), no la suite — pero la suite se corre igual antes del commit, porque el gate es el gate.

Todo lo que sigue está copiado del árbol: firmas de `src/speechtotext/audio/*.py` y `src/speechtotext/asr/types.py`. **No inventes campos ni valores por defecto.** Si algo del texto no cuadra con el código, gana el código: arregla el texto y anótalo en el reporte.

- [ ] **Step 1: Insertar la sección `Capa audio/`**

Va **justo antes** de la línea `## Evidencia de voz` (hoy en `docs/api.md:92`), y la sección "Evidencia de voz" que ya existe se queda donde está, detrás. Inserta este texto tal cual, seguido de una línea en blanco:

````markdown
## Capa `audio/`: medidas sobre la señal, sin veredicto

`speechtotext.audio` mide y transforma audio; **nunca decide si transcribir**. Todo lo
que exporta es inmutable y se valida al construirse: un tipo mal formado revienta donde
se crea, no tres capas más abajo.

```python
from speechtotext.audio import decode_audio, AudioDecodeError

with open("reunion.m4a", "rb") as stream:
    view = decode_audio(stream, sample_rate=16000)   # -> AudioView
samples = view.samples                               # float32 mono, de solo lectura
```

### Decodificación

- **`decode_audio(stream, *, sample_rate, av_module=None) -> AudioView`** — entra un
  stream binario *seekable* (no una ruta: quien abre, cierra), sale un `AudioView` mono
  float32 al `sample_rate` pedido. `av_module` es el seam de los tests.
- **`AudioDecodeError(RuntimeError)`** — el archivo no se pudo abrir o no trae audio.

### Un clip y sus vistas

El mismo audio se mide, se identifica y se transcribe con preprocesados distintos.
`AudioClip` los guarda juntos con su proveniencia, para que cada número se pueda rastrear
hasta las muestras de las que salió.

- **`AudioClip(started_at, ended_at, source_id, speech_regions, quality, views)`** —
  `.view(name)` devuelve la vista pedida y lanza `KeyError` si no está o no existe.
  Valida al construirse que las duraciones de las vistas y `quality.duration_ms` cuadren
  con el clip, y que las regiones de habla no se solapen ni se salgan.
- **`AudioViews(capture, analysis, asr, identity=None, spoof=None)`** — las tres primeras
  son obligatorias.
- **`AudioViewName`** — `Literal["capture", "analysis", "identity", "spoof", "asr"]`.
- **`AudioView`** — `samples` (float32 mono, respaldado por `bytes`: no se puede escribir),
  `sample_rate`, `provenance`. **Sin constructor público**: se crea con
  `AudioView.capture(samples, sample_rate, *, step)` o con
  `AudioView.derive(parent, samples, *, sample_rate=None, steps, models=(), thresholds=None)`.
  Propiedades: `duration_s` y `pipeline_fingerprint`.
- **`SpeechRegion(start_s, end_s)`** — ordenable; exige tiempos finitos y `0 <= start_s < end_s`.

### Calidad y ganancia

```python
from speechtotext.audio import apply_fixed_gain, compute_audio_quality

gain = apply_fixed_gain(samples, 6.0)
report = compute_audio_quality(samples, gain.samples, 16000, regions,
                               requested_gain_db=6.0, applied_gain_db=gain.applied_gain_db)
```

- **`compute_audio_quality(capture, processed, sample_rate, speech_regions, *, requested_gain_db, applied_gain_db, dropped_frames=0, discontinuities=0) -> AudioQualityReport`**
- **`AudioQualityReport`** — `duration_ms`, `effective_voice_ms`, `input_rms_dbfs`,
  `processed_rms_dbfs`, `peak_dbfs`, `clipping_ratio`, `noise_floor_dbfs`, `snr_db`,
  `requested_gain_db`, `applied_gain_db`, `dropped_frames`, `discontinuities`, `warnings`.
  `noise_floor_dbfs` y `snr_db` son `None` cuando no hubo silencio suficiente para
  medirlos: **`None` no es cero**, la misma regla que gobierna la evidencia de voz.
- **`apply_fixed_gain(samples, gain_db, *, max_gain_db=18.0, peak_limit_dbfs=-1.0) -> GainResult`**
- **`GainResult(samples, requested_gain_db, applied_gain_db, limited)`** — `limited` es
  `True` cuando el limitador tuvo que recortar la ganancia pedida.

### Puerta de calidad

Decide si un clip merece inferencia, contra umbrales que pone quien llama. La librería no
trae umbrales por defecto: medir es suyo, decidir es de quien la usa.

- **`QualityThresholds(min_effective_voice_ms, min_processed_rms_dbfs, min_snr_db, max_clipping_ratio, max_dropped_frames=0, max_discontinuities=0)`**
- **`evaluate_pre_inference(report, thresholds) -> PreInferenceDecision`**
- **`PreInferenceDecision(eligible, reason_codes)`**
- **`QualityReason`** — `Literal["silence", "too_short", "level_too_low", "snr_unavailable",
  "snr_too_low", "clipping", "dropped_audio", "discontinuous_audio"]`. La puerta acumula
  **todas** las razones que aplican, no la primera.

### Proveniencia

La huella de un pipeline de audio: qué pasos, qué modelos, qué umbrales. Sirve para afirmar
que dos resultados salieron del mismo tratamiento sin guardar el audio.

- **`PipelineStep(name, version, parameters)`** — `parameters` tiene que ser JSON
  serializable; se congela al construirse.
- **`ModelRef(model_id, fingerprint)`** — `fingerprint` es un sha256 en hex minúsculas de
  64 caracteres. La proveniencia no sabe de manifiestos ni de sistemas de archivos: quien
  tenga un modelo verificado lo reduce a esto, y así la huella se puede calcular en
  cualquier máquina.
- **`PipelineProvenance`** — `sample_rate`, `parent_fingerprint`, `steps`,
  `model_fingerprints`, `thresholds`. **Sin constructor público**:
  `PipelineProvenance.capture(*, sample_rate, step, models=(), thresholds=None)` y
  `PipelineProvenance.derive(parent, *, sample_rate, steps, models=(), thresholds=None)`.
  Propiedad `fingerprint` (sha256 del payload ordenado), más `to_dict()` y
  `from_dict(data, *, parent, models)`.

---
````

- [ ] **Step 2: Nombrar los cinco símbolos sueltos que faltan**

Cuatro ediciones pequeñas, en cuatro sitios distintos del documento.

**(a)** En la sección `## Capa asr/`, dentro del bloque ```` ```python ```` que hoy describe `AsrBackend` (hoy `docs/api.md:281-288`), **debajo** de la línea que cierra el bloque, añade este párrafo nuevo antes de la línea que empieza `Entra **float32 mono**`:

```markdown
`TranscriptionResult.segments` son `TranscriptionSegment(start, end, text, words,
native_signals)`, y sus `words` son `TranscriptionWord(text, start, end, confidence)` —
`confidence` es `None` o un número entre 0 y 1, nunca fabricado. `Cap` es el
`Literal["honrado", "degradado", "rechazado"]` del que se arman los `Caps`.
```

(Campos verificados contra `src/speechtotext/asr/types.py:80-105` y `:151-158`. Si no
cuadran, gana el código: corrige el texto y anótalo en el reporte.)

**(b)** En la sección `## transcribe()`, en el párrafo que empieza "Un archivo (o muestras
16 kHz mono) entra", cambia `engine` y `diarization` de la enumeración para que nombren sus
tipos:

- de: `` `gaps`, `engine`, `request` (la efectiva, tras CAPS), `warnings` y `diarization`. ``
- a: `` `gaps`, `engine` (un `EngineInfo`, con `.to_dict()` para el JSON), `request` (la efectiva, tras CAPS), `warnings` y `diarization` (un `DiarizationReport` o `None`). ``

**(c)** En la sección `## Identificación` (hoy `docs/api.md:245-250`), el nombre
`assign_names` no aparece: la prosa describe el algoritmo pero nunca nombra la función.
Reemplaza el párrafo entero

```markdown
`speechtotext.speakers.identify` compara por coseno y asigna nombres con un
greedy: ordena todos los pares posibles de mayor a menor puntaje, corta por
debajo del umbral, y asigna sin reusar ni un hablante ni un nombre. El umbral por
defecto del CLI es `0.5`.
```

por

```markdown
`assign_names(clusters, enrolled, threshold) -> dict[str, str]` mapea cada `speaker_id`
anónimo a un nombre registrado, con un greedy: ordena todos los pares posibles de mayor a
menor coseno, corta por debajo del umbral, y asigna sin reusar ni un hablante ni un
nombre. El umbral por defecto del CLI es `0.5`. `cosine` está expuesto pero es
implementación: puede cambiar sin aviso.
```

- [ ] **Step 3: Verificar el conteo — esta es la prueba de la tarea**

Corre esto desde la raíz del repo:

````bash
.venv/Scripts/python.exe - <<'PY'
import re, pathlib, importlib
doc = pathlib.Path("docs/api.md").read_text(encoding="utf-8")
BLOQUE = re.compile(r"```.*?```", re.S)
bloques = BLOQUE.findall(doc); resto = BLOQUE.sub("\n", doc)
trozos = bloques + re.findall(r"`([^`\n]+)`", resto) + re.findall(r"^#{1,6}\s+(.+)$", resto, re.M)
tok = {x for y in trozos for x in re.findall(r"[A-Za-z_][A-Za-z0-9_]*", y)}
for mod in ("speechtotext.asr", "speechtotext.audio"):
    falta = [n for n in importlib.import_module(mod).__all__ if n not in tok]
    print(f"{mod}: faltan {falta}")
sueltos = ["EngineInfo", "DiarizationReport", "assign_names"]
print("sueltos que faltan:", [n for n in sueltos if n not in tok])
PY
````

Esperado, las tres líneas:

```
speechtotext.asr: faltan []
speechtotext.audio: faltan []
sueltos que faltan: []
```

Si alguna lista sale con nombres, vuelve al Paso 1 o 2 y documéntalos. **No** los saques de
`__all__` para que el conteo cuadre: eso es cambiar el contrato para aprobar el examen.

- [ ] **Step 4: Gate**

```bash
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

Esperado: `435 passed`. Esta tarea no toca código; si el número cambia, algo más pasó y hay
que averiguar qué antes de seguir.

- [ ] **Step 5: Commit**

```bash
git add docs/api.md
git commit -m "docs(api): documentar la capa audio/ entera y los tipos de asr/ que faltaban"
```
---

### Task 2: `tests/test_api_contract.py` — el mapa se rompe con ruido

**Files:**
- Create: `tests/test_api_contract.py`
- Test: es el test.

**Interfaces:**
- Consumes: `docs/api.md` tal como lo dejó la Tarea 1 (23 nombres nuevos).
- Produces: nada que otra tarea importe. Pero la Tarea 5 **añade un párrafo a `api.md`** y este test lo vigila desde aquí.

El ciclo va invertido a propósito (ver "Orden y por qué"): el documento ya está completo, así
que el test nace en verde. El Paso 2 es el que demuestra que muerde — sin él, esta tarea
entrega un test que podría no estar comprobando nada.

- [ ] **Step 1: Escribir el test**

Crea `tests/test_api_contract.py` con exactamente esto:

````python
"""El mapa se rompe con ruido: si `docs/api.md` y el código se separan, esto lo dice.

Dos direcciones, ninguna con parser de firmas:

  (a) mecánica — todo nombre exportado en un `__all__` público aparece en api.md.
  (b) curada   — toda ruta de CONTRATO importa y aparece en api.md.

La (a) caza el modo de fallo que de verdad ocurrió (exportar algo y no documentarlo:
llegaron a ser 17 de 20 en `audio`). La (b) caza el inverso, documentar lo que ya no
existe. Un parser de firmas Markdown se pudriría más rápido que lo que vigila.

`core/` y `speakers/` no tienen `__all__` — se importan por submódulo — así que su
superficie pública vive en CONTRATO, nombre a nombre. Añadir algo ahí es declararlo
contrato: sale en el CHANGELOG cuando cambie.
"""
import importlib
import re
from pathlib import Path

import pytest

API_MD = Path(__file__).resolve().parents[1] / "docs" / "api.md"

MODULOS_CON_ALL = ("speechtotext.asr", "speechtotext.audio")

CONTRATO = (
    "speechtotext.core.transcribe.transcribe",
    "speechtotext.core.transcribe.Transcript",
    "speechtotext.core.transcribe.Progress",
    "speechtotext.core.transcribe.EngineInfo",
    "speechtotext.core.transcribe.DiarizationReport",
    "speechtotext.core.transcribe.load_audio",
    "speechtotext.core.probe.machine",
    "speechtotext.core.probe.choose_route",
    "speechtotext.core.probe.Machine",
    "speechtotext.core.probe.Route",
    "speechtotext.core.models.data_dir",
    "speechtotext.core.models.installed",
    "speechtotext.core.models.ensure",
    "speechtotext.core.models.remove",
    "speechtotext.core.models.remote_size",
    "speechtotext.core.models.ModelInfo",
    "speechtotext.core.formats.write_json",
    "speechtotext.core.formats.is_suspect",
    "speechtotext.speakers.registry.enroll",
    "speechtotext.speakers.registry.list_voices",
    "speechtotext.speakers.registry.get_embeddings",
    "speechtotext.speakers.registry.remove",
    "speechtotext.speakers.identify.assign_names",
    "speechtotext.speakers.diarization.diarize",
    "speechtotext.asr.base.AsrError",
    "speechtotext.asr.faster_whisper.FasterWhisperBackend",
    "speechtotext.asr.whispercpp.WhisperCppBackend",
)

_BLOQUE = re.compile(r"```.*?```", re.S)


def _nombrados(texto: str) -> set[str]:
    """Identificadores que api.md nombra en código o en un encabezado.

    Cuenta los bloques cercados, los `code span` sueltos y los encabezados; la prosa
    no cuenta, porque nombrar algo de pasada no es documentarlo. Se tokeniza en vez
    de buscar la subcadena: si no, `AudioView` pasaría gratis porque existe
    `AudioViewName`.
    """
    bloques = _BLOQUE.findall(texto)
    resto = _BLOQUE.sub("\n", texto)
    trozos = (bloques
              + re.findall(r"`([^`\n]+)`", resto)
              + re.findall(r"^#{1,6}\s+(.+)$", resto, re.M))
    return {t for trozo in trozos for t in re.findall(r"[A-Za-z_][A-Za-z0-9_]*", trozo)}


@pytest.fixture(scope="module")
def nombrados() -> set[str]:
    return _nombrados(API_MD.read_text(encoding="utf-8"))


@pytest.mark.parametrize("modulo", MODULOS_CON_ALL)
def test_todo_lo_exportado_esta_documentado(modulo, nombrados):
    exportados = importlib.import_module(modulo).__all__
    faltan = sorted(n for n in exportados if n not in nombrados)
    assert not faltan, (
        f"{modulo}.__all__ exporta {len(faltan)} nombres que docs/api.md no menciona: "
        f"{', '.join(faltan)}. Documéntalos, o sácalos de __all__ si no son contrato."
    )


@pytest.mark.parametrize("ruta", CONTRATO)
def test_lo_documentado_existe_y_se_importa(ruta, nombrados):
    modulo, _, nombre = ruta.rpartition(".")
    objeto = importlib.import_module(modulo)
    assert hasattr(objeto, nombre), (
        f"docs/api.md promete {ruta} y no existe. Si se renombró, renómbralo también "
        f"en el documento y en el CHANGELOG (rompe)."
    )
    assert nombre in nombrados, f"{ruta} es contrato y docs/api.md no lo nombra"


def test_el_tokenizador_no_regala_prefijos():
    """La trampa que hay que evitar: `AudioViewName` no puede documentar `AudioView`."""
    tokens = _nombrados("Ver `AudioViewName` para las vistas.")
    assert "AudioViewName" in tokens
    assert "AudioView" not in tokens


def test_la_prosa_no_documenta():
    """Nombrar algo en una frase no es documentarlo: tiene que estar en código."""
    assert _nombrados("AudioClip es la entrada por clip.") == set()
    assert "AudioClip" in _nombrados("- **`AudioClip(started_at, ...)`** — la entrada.")
````

`re.findall(r"[A-Za-z_][A-Za-z0-9_]*", "AudioViewName")` devuelve `["AudioViewName"]`, una
sola pieza: por eso tokenizar evita el regalo por subcadena, y por eso el primero de esos
dos tests existe.

- [ ] **Step 2: Correr solo los dos tests del tokenizador**

```bash
.venv/Scripts/python.exe -m pytest tests/test_api_contract.py -q -p no:cacheprovider -k "tokenizador or prosa"
```

Esperado: `2 passed`. Si `test_la_prosa_no_documenta` falla, el regex de encabezados o el
de *code spans* está mordiendo texto plano: arréglalo antes de seguir, porque un
tokenizador demasiado generoso convierte las dos direcciones en decoración.

- [ ] **Step 3: Correr los tests nuevos**

```bash
.venv/Scripts/python.exe -m pytest tests/test_api_contract.py -q -p no:cacheprovider
```

Esperado: **31 passed** (2 módulos + 27 rutas de `CONTRATO` + 2 del tokenizador). Si algo
falla en `test_todo_lo_exportado_esta_documentado` o en
`test_lo_documentado_existe_y_se_importa`, la Tarea 1 dejó un hueco: documéntalo en
`docs/api.md` y vuelve a correr. **No** quites nombres de `CONTRATO` ni de `__all__` para
aprobar.

- [ ] **Step 4: Demostrar que el test muerde (el rojo, invertido)**

Esta tarea nació verde, así que hay que provocar el rojo a mano para probar que el
mecanismo funciona. Dos ediciones temporales con el editor, cada una revertida antes de
la siguiente.

**(a) Exportar sin documentar tiene que fallar.** Añade al final de
`src/speechtotext/audio/__init__.py`:

```python
__all__ += ["SimboloDePrueba"]
```

Corre `.venv/Scripts/python.exe -m pytest tests/test_api_contract.py -q -p no:cacheprovider`.

Esperado: falla `test_todo_lo_exportado_esta_documentado[speechtotext.audio]` y el mensaje
nombra literalmente `SimboloDePrueba`. **Quita la línea** y confirma que vuelve a verde.

**(b) Documentar lo que no existe tiene que fallar.** En `tests/test_api_contract.py`,
añade a `CONTRATO`, justo debajo de `"speechtotext.core.probe.machine",`:

```python
    "speechtotext.core.probe.sondea_la_maquina",
```

Corre el mismo comando.

Esperado: falla con `docs/api.md promete speechtotext.core.probe.sondea_la_maquina y no
existe`. **Quita la línea** y confirma que vuelve a **31 passed**.

Anota en el reporte los dos mensajes de fallo que viste. Si alguno **no** falló, el test
no está comprobando lo que dice: arréglalo antes de seguir.

- [ ] **Step 5: Gate**

```bash
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

Esperado: **466 passed** (435 + 31).

- [ ] **Step 6: Commit**

```bash
git add tests/test_api_contract.py
git commit -m "test(api): el contrato se rompe con ruido — __all__ y lista curada contra api.md"
```

---

### Task 3: fixture sintética — ni habla humana real ni rutas de máquina

**Files:**
- Modify: `tests/fixtures/whispercpp_ojf.json`
- Modify: `tests/test_whispercpp_backend.py`
- Modify: `tests/test_formats_speaker.py`

**Interfaces:**
- Consumes: nada de tareas anteriores.
- Produces: nada. Tarea aislada.

La fixture de hoy es la salida `-ojf` real de una grabación de habla humana, y su
`params.model` lleva la ruta completa de la máquina de desarrollo (`C:\Users\<usuario>\...`).
Las dos cosas son bloqueadores de lanzamiento: la primera por §7 ("cero habla humana real en
fixtures"), la segunda porque `simon` está en la tabla de palabras prohibidas de §9.5.

Se reemplaza el **contenido**, no el mecanismo: sigue siendo un archivo en
`tests/fixtures/`, con la misma forma exacta de `-ojf` (ver R6).

- [ ] **Step 1: Escribir el test que guarda la propiedad**

En `tests/test_whispercpp_backend.py`, añade este test **justo debajo** de la definición de
`FIXTURE` (hoy línea 19) — antes que cualquier otra función:

```python
def test_la_fixture_es_sintetica_y_sin_rutas_de_maquina():
    """Guarda de lanzamiento: esta fixture viaja al repo público. Si alguien la regenera
    pegando una salida real de su máquina, se entera aquí y no en el filter-repo."""
    crudo = FIXTURE.read_text(encoding="utf-8")
    assert "\\" not in crudo, "ruta de Windows en la fixture"
    assert "Users" not in crudo, "ruta de máquina en la fixture"
    payload = json.loads(crudo)
    textos = [s["text"].strip() for s in payload["transcription"]]
    assert len(textos) == 6
    assert all(t.startswith("Segmento ") for t in textos), textos
```

- [ ] **Step 2: Correrlo y verlo fallar**

```bash
.venv/Scripts/python.exe -m pytest tests/test_whispercpp_backend.py::test_la_fixture_es_sintetica_y_sin_rutas_de_maquina -q -p no:cacheprovider
```

Esperado: FALLA en el primer assert, `ruta de Windows en la fixture`.

- [ ] **Step 3: Reemplazar la fixture**

Sobrescribe `tests/fixtures/whispercpp_ojf.json` **entero** con esto. Misma forma que la
salida real de `whisper-cli -ojf`: `systeminfo`, `model`, `params`, `result`,
`transcription[]` con `timestamps` / `offsets` / `text` / `tokens[]`.

```json
{
  "systeminfo": "WHISPER : COREML = 0 | OPENVINO = 0 | CUDA : ARCHS = 500,610,700,750,800,860,890,900 | PEER_MAX_BATCH_SIZE = 128 | CPU : SSE3 = 1 | SSSE3 = 1 | AVX = 1 | AVX2 = 1 | F16C = 1 | FMA = 1 | BMI2 = 1 | OPENMP = 1 | REPACK = 1 | ",
  "model": {
    "type": "large",
    "multilingual": true,
    "vocab": 51866,
    "audio": { "ctx": 1500, "state": 1280, "head": 20, "layer": 32 },
    "text": { "ctx": 448, "state": 1280, "head": 20, "layer": 32 },
    "mels": 128,
    "ftype": 8
  },
  "params": {
    "model": "models/ggml-large-v3-q5_0.bin",
    "language": "es",
    "translate": false
  },
  "result": { "language": "es" },
  "transcription": [
    {
      "timestamps": { "from": "00:00:00,000", "to": "00:00:19,920" },
      "offsets": { "from": 0, "to": 19920 },
      "text": " Segmento uno de la pista de prueba.",
      "tokens": [
        { "text": "[_BEG_]", "timestamps": { "from": "00:00:00,000", "to": "00:00:00,000" }, "offsets": { "from": 0, "to": 0 }, "id": 50365, "p": 0.400058, "t_dtw": -1 },
        { "text": " Segmento", "timestamps": { "from": "00:00:00,000", "to": "00:00:03,200" }, "offsets": { "from": 0, "to": 3200 }, "id": 9081, "p": 0.921144, "t_dtw": -1 }
      ]
    },
    {
      "timestamps": { "from": "00:00:19,920", "to": "00:00:33,440" },
      "offsets": { "from": 19920, "to": 33440 },
      "text": " Segmento dos, tono de mil hercios.",
      "tokens": [
        { "text": "[_BEG_]", "timestamps": { "from": "00:00:19,920", "to": "00:00:19,920" }, "offsets": { "from": 19920, "to": 19920 }, "id": 50365, "p": 0.512300, "t_dtw": -1 },
        { "text": " Segmento", "timestamps": { "from": "00:00:19,920", "to": "00:00:22,400" }, "offsets": { "from": 19920, "to": 22400 }, "id": 9081, "p": 0.887012, "t_dtw": -1 }
      ]
    },
    {
      "timestamps": { "from": "00:00:33,440", "to": "00:00:47,000" },
      "offsets": { "from": 33440, "to": 47000 },
      "text": " Segmento tres, silencio y ruido rosa.",
      "tokens": [
        { "text": "[_BEG_]", "timestamps": { "from": "00:00:33,440", "to": "00:00:33,440" }, "offsets": { "from": 33440, "to": 33440 }, "id": 50365, "p": 0.478900, "t_dtw": -1 },
        { "text": " Segmento", "timestamps": { "from": "00:00:33,440", "to": "00:00:36,100" }, "offsets": { "from": 33440, "to": 36100 }, "id": 9081, "p": 0.903771, "t_dtw": -1 }
      ]
    },
    {
      "timestamps": { "from": "00:00:47,000", "to": "00:00:58,120" },
      "offsets": { "from": 47000, "to": 58120 },
      "text": " Segmento cuatro, barrido de frecuencia.",
      "tokens": [
        { "text": "[_BEG_]", "timestamps": { "from": "00:00:47,000", "to": "00:00:47,000" }, "offsets": { "from": 47000, "to": 47000 }, "id": 50365, "p": 0.533412, "t_dtw": -1 },
        { "text": " Segmento", "timestamps": { "from": "00:00:47,000", "to": "00:00:49,640" }, "offsets": { "from": 47000, "to": 49640 }, "id": 9081, "p": 0.914005, "t_dtw": -1 }
      ]
    },
    {
      "timestamps": { "from": "00:00:58,120", "to": "00:01:11,600" },
      "offsets": { "from": 58120, "to": 71600 },
      "text": " Segmento cinco, tono de cuatrocientos cuarenta hercios.",
      "tokens": [
        { "text": "[_BEG_]", "timestamps": { "from": "00:00:58,120", "to": "00:00:58,120" }, "offsets": { "from": 58120, "to": 58120 }, "id": 50365, "p": 0.461277, "t_dtw": -1 },
        { "text": " Segmento", "timestamps": { "from": "00:00:58,120", "to": "00:01:00,900" }, "offsets": { "from": 58120, "to": 60900 }, "id": 9081, "p": 0.896430, "t_dtw": -1 }
      ]
    },
    {
      "timestamps": { "from": "00:01:11,600", "to": "00:01:24,000" },
      "offsets": { "from": 71600, "to": 84000 },
      "text": " Segmento seis, fin de la pista.",
      "tokens": [
        { "text": "[_BEG_]", "timestamps": { "from": "00:01:11,600", "to": "00:01:11,600" }, "offsets": { "from": 71600, "to": 71600 }, "id": 50365, "p": 0.502844, "t_dtw": -1 },
        { "text": " Segmento", "timestamps": { "from": "00:01:11,600", "to": "00:01:14,300" }, "offsets": { "from": 71600, "to": 74300 }, "id": 9081, "p": 0.908912, "t_dtw": -1 }
      ]
    }
  ]
}
```

Los `tokens` bajan de todos los del audio real a dos por segmento: `parse_ojf` no los lee
(lo prueba `test_parser_filtra_segmentos_de_texto_vacio`, que le pasa un payload **sin**
`tokens` y funciona), y la forma se conserva igual para el día que alguien los lea.

- [ ] **Step 4: Ajustar los dos asserts de texto**

En `tests/test_whispercpp_backend.py`, dentro de `test_transcribe_parsea_la_fixture_real`:

```python
    assert result.segments[0].text == " Ay, gracias. Gracias por haberme dejado tantos años."
```

pasa a

```python
    assert result.segments[0].text == " Segmento uno de la pista de prueba."
```

Y renombra la función y su intención, porque ya no parsea nada "real":

```python
def test_transcribe_parsea_la_fixture():
```

En `tests/test_formats_speaker.py`, dentro de `test_pipeline_whispercpp_fixture_a_write_json`:

```python
    assert data["segments"][0]["text"] == "Ay, gracias. Gracias por haberme dejado tantos años."
```

pasa a

```python
    assert data["segments"][0]["text"] == "Segmento uno de la pista de prueba."
```

Y en el docstring de esa misma función, cambia `la fixture real de -ojf` por
`la fixture sintética de -ojf`.

Los tiempos (`0.0`, `19.92`) y el conteo (`6` segmentos) **no cambian**: la fixture nueva
conserva los mismos offsets, así que esos asserts siguen valiendo tal cual.

- [ ] **Step 5: Correr los dos archivos**

```bash
.venv/Scripts/python.exe -m pytest tests/test_whispercpp_backend.py tests/test_formats_speaker.py -q -p no:cacheprovider
```

Esperado: todo verde, incluido el test nuevo del Paso 1.

- [ ] **Step 6: Barrido**

```bash
git grep -nI -e 'Users' -e 'gracias' -- tests/fixtures/
```

Esperado: sin salida.

- [ ] **Step 7: Gate**

```bash
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

Esperado: **467 passed** (466 + 1).

- [ ] **Step 8: Commit**

```bash
git add tests/fixtures/whispercpp_ojf.json tests/test_whispercpp_backend.py tests/test_formats_speaker.py
git commit -m "test(fixtures): fixture de whisper.cpp sintética, sin habla real ni rutas de máquina"
```
---

### Task 4: CI — el extra roto, la matriz de seis y el job `package`

**Files:**
- Modify: `.github/workflows/tests.yml`

**Interfaces:**
- Consumes: nada.
- Produces: nada que el código importe. La Tarea 6 pone el badge que apunta a este workflow.

El workflow existe y nunca ha pasado: instala `pip install -e ".[dev,evaluation]"` y el extra
`evaluation` se fue del `pyproject` en `59730b8` ("el corte"). Además cubre dos combinaciones
donde el spec pide tres sistemas, y no comprueba que el paquete se pueda instalar.

**Limitación conocida de esta tarea:** un workflow no se puede verificar en local. El Paso 3
corre el **equivalente local del job `package`**, que es la parte con riesgo real (que el
wheel no instale). La matriz solo la prueba GitHub cuando se abra el PR; por eso el archivo
va escrito entero abajo y hay que leerlo contra el de hoy, no aplicarlo a ciegas.

- [ ] **Step 1: Reemplazar `.github/workflows/tests.yml` entero**

```yaml
# Corre la suite fuera de la máquina de desarrollo.
#
# El motivo no es ceremonial: este proyecto se instala en modo editable y su .pth ha
# llegado a apuntar a un árbol distinto del que se estaba editando, con lo que una
# verificación "verde" corría código viejo sin que nadie lo notara. Aquí no hay venv
# heredado ni worktree ambiguo: se clona, se instala y se corre.
name: tests

on:
  push:
    branches: [main]
  pull_request:

# Solo lectura: este workflow corre tests, no escribe nada en el repo. El default
# del token es mas amplio y aqui no hace falta.
permissions:
  contents: read

jobs:
  pytest:
    name: ${{ matrix.os }} · py${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      # Sin fail-fast: si rompe en un solo sistema operativo, eso ES el dato.
      fail-fast: false
      matrix:
        # Los tres sistemas por los dos extremos de lo soportado. 3.11 es el piso que
        # declara requires-python y que nadie prueba en local; 3.14 es lo que corre la
        # maquina de desarrollo. Lo de en medio se interpola. Si alguna rueda (av, numpy)
        # todavia no existe para 3.14 en algun sistema, se baja ESE par a 3.13 y se anota
        # aqui; no la matriz entera.
        os: [ubuntu-latest, windows-latest, macos-latest]
        python: ["3.11", "3.14"]

    steps:
      - uses: actions/checkout@v4
        with:
          # Sin esto el token queda en la config local de git mientras corre codigo
          # que viene del PR. No se hace push desde aqui, asi que no se necesita.
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: pyproject.toml

      # Sin los extras `diarize` ni `mcp`: ningun test los importa. torch y pyannote
      # entran por import perezoso dentro de speakers/diarization.py, y el SDK de MCP
      # solo lo toca serve(); los tests que los tocan siembran un doble en sys.modules
      # en vez de cargarlos. Instalarlos anadiria cerca de un giga de descarga para no
      # ejecutar una linea mas.
      - name: Instalar
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      # La suite entera corre sin GPU, sin red y sin modelos: tests/conftest.py le da a
      # cada test una maquina sondeada fija y un SPEECHTOTEXT_HOME temporal.
      - name: Tests
        run: pytest -q

  package:
    # Que la suite pase sobre el arbol no prueba que el paquete sirva: un subpaquete que
    # no entra en packages.find, un entry point mal escrito, un modulo que solo se
    # resolvia por el .pth del editable. Aqui se instala como lo instala un usuario.
    name: wheel + instalación limpia
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          # El piso: si el wheel se construye e instala en 3.11, se instala en todas.
          python-version: "3.11"

      - name: Construir el wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build --wheel

      # Venv aparte y solo el wheel: si algo del paquete dependia del arbol, revienta aqui.
      - name: Instalar el wheel en un venv limpio
        run: |
          python -m venv /tmp/limpio
          /tmp/limpio/bin/pip install dist/*.whl

      # `probe` es la prueba de humo honesta: importa core.probe y core.enginepin, sondea
      # la maquina (sin GPU ni whisper.cpp en el runner) e imprime la ruta que elegiria.
      # No baja modelos, no toca red, y sale 0 incluso si la RAM no diera para large-v3.
      - name: Humo
        run: |
          /tmp/limpio/bin/speechtotext --help
          /tmp/limpio/bin/speechtotext probe
```

Cambios respecto al de hoy, para que la revisión los pueda contar:
1. `".[dev,evaluation]"` → `".[dev]"` (el extra ya no existe).
2. `include:` de dos combinaciones → producto de 3 sistemas × 2 versiones.
3. `timeout-minutes: 15` → `20` (seis jobs, macOS es el más lento).
4. Comentarios de la matriz y de la instalación reescritos (mencionaban `evaluation`, `api`, y "windows + 3.14 es el entorno real" bajo un `include`).
5. Job `package` nuevo.

- [ ] **Step 2: Verificar que el YAML no tiene tabs ni indentación rota**

```bash
git diff --check .github/workflows/tests.yml
grep -nP "\t" .github/workflows/tests.yml
```

Esperado: sin salida en ninguno de los dos.

- [ ] **Step 3: Correr el equivalente local del job `package`**

Esta es la verificación real de la tarea. En Windows, desde la raíz del repo:

```bash
.venv/Scripts/python.exe -m pip install --upgrade build
rm -rf dist
.venv/Scripts/python.exe -m build --wheel
ls dist/
```

Esperado: un `dist/speechtotext-0.6.0.dev0-py3-none-any.whl`.

```bash
rm -rf /tmp/stt-limpio
.venv/Scripts/python.exe -m venv /tmp/stt-limpio
/tmp/stt-limpio/Scripts/python.exe -m pip install --upgrade pip
/tmp/stt-limpio/Scripts/python.exe -m pip install dist/*.whl
/tmp/stt-limpio/Scripts/speechtotext.exe --help
/tmp/stt-limpio/Scripts/speechtotext.exe probe
```

(Git Bash mapea `/tmp`. Si tu shell no lo tiene, usa cualquier carpeta fuera del repo.)

Esperado: `--help` lista los subcomandos y sale 0; `probe` imprime el bloque `platform /
cpu_count / ram_gb / cuda / gpu / vram_free / whispercpp` y dos líneas de ruta
(`large-v3` y `small`), y sale 0.

Pega la salida de `probe` en el reporte. Si `--help` o `probe` revientan con un
`ModuleNotFoundError`, **ese es exactamente el bug que el job existe para cazar**: falta un
paquete en `[tool.setuptools.packages.find]` o un `__init__.py`. Arréglalo en esta misma
tarea y anótalo.

```bash
rm -rf dist /tmp/stt-limpio
```

`dist/` ya está en `.gitignore`; bórralo igual para no dejar basura.

- [ ] **Step 4: Gate**

```bash
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

Esperado: **467 passed**. Esta tarea no toca código de la librería.

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/tests.yml
git commit -m "ci: matriz de tres sistemas, arreglar el extra que ya no existe y empaquetar el wheel"
```

---

### Task 5: `speechtotext mcp` — cuatro herramientas sobre stdio

**Files:**
- Create: `src/speechtotext/cli/mcp_server.py`
- Create: `tests/test_mcp.py`
- Modify: `src/speechtotext/cli/app.py` (comando nuevo)
- Modify: `pyproject.toml` (extra `[mcp]`)

**Interfaces:**
- Consumes: `core.transcribe.transcribe`, `core.formats.write_json`, `core.finder.load_or_build_index` / `finder.search`, `speakers.registry.list_voices`, `core.probe.machine` / `choose_route`. Todas existen hoy con las firmas de la tabla "Callers verificados"; ninguna cambia.
- Produces: `speechtotext.cli.mcp_server.HERRAMIENTAS` (tupla de las cuatro funciones) y `serve()`. La Tarea 6 los documenta.

Es una capa fina: envolver y devolver diccionarios. Las cuatro herramientas son funciones
planas con anotaciones — el SDK deriva el esquema JSON de las anotaciones — y **no importan
`mcp`**, para poder probarlas sin el extra instalado. `serve()` es lo único que lo toca (R7).

**El servidor no imprime nunca.** Un servidor stdio que escriba en stdout rompe el
protocolo: por eso `on_progress=None` y ni un `print` ni un `console.print` en todo el
módulo.

- [ ] **Step 1: Escribir los tests que fallan**

Crea `tests/test_mcp.py`:

```python
"""Las cuatro herramientas MCP, probadas sin el SDK.

El extra [mcp] no está en [dev] a propósito: las herramientas son funciones planas que
no lo tocan, y serve() —lo único que lo importa— se prueba sembrando un doble en
sys.modules, el mismo patrón que test_models.py usa con huggingface_hub.
"""
import json
import sys
from pathlib import Path
from types import ModuleType, SimpleNamespace

import pytest

from speechtotext.cli import mcp_server
from speechtotext.core import finder
from speechtotext.core import transcribe as core_transcribe
from speechtotext.core.segments import LabeledSegment
from speechtotext.core.transcribe import EngineInfo
from speechtotext.speakers import registry


def _transcript(segments):
    return SimpleNamespace(
        segments=segments,
        language="es",
        language_probability=0.97,
        duration=12.5,
        speech_s=11.0,
        gaps=[],
        engine=EngineInfo("whispercpp", "whisper.cpp v1.9.1", "large-v3", "q5_0", "cuda"),
        request=None,
        warnings=("whispercpp: empty_transcript",),
        diarization=None,
    )


def test_transcribe_escribe_el_json_al_lado_y_devuelve_el_texto(monkeypatch, tmp_path):
    audio = tmp_path / "reunion.mp4"
    audio.write_bytes(b"no importa: transcribe va stubbeado")
    visto = {}

    def falso(ruta, **kw):
        visto.update(ruta=ruta, kw=kw)
        return _transcript([LabeledSegment(0.0, 3.0, " hola"),
                            LabeledSegment(3.0, 6.0, " qué tal")])

    monkeypatch.setattr(core_transcribe, "transcribe", falso)

    salida = mcp_server.transcribe(str(audio))

    assert salida["text"] == "hola\nqué tal"
    assert salida["json"] == str(tmp_path / "reunion.json")
    assert salida["language"] == "es"
    assert salida["duration"] == 12.5
    assert salida["warnings"] == ["whispercpp: empty_transcript"]
    # el JSON existe de verdad y lo escribió el mismo writer que el CLI
    data = json.loads(Path(salida["json"]).read_text(encoding="utf-8"))
    assert data["language"] == "es" and len(data["segments"]) == 2
    assert data["engine"]["name"] == "whispercpp"
    # sin progreso: un callback que imprima rompe el protocolo stdio
    assert visto["kw"]["on_progress"] is None
    assert visto["kw"]["model"] == "large-v3" and visto["kw"]["language"] == "auto"
    assert visto["kw"]["diarize"] is False


def test_find_devuelve_las_regiones_sin_extraer(monkeypatch, tmp_path):
    audio = tmp_path / "largo.m4a"
    audio.write_bytes(b"x")
    monkeypatch.setattr(finder, "load_or_build_index",
                        lambda a, m, r=False: ([{"start": 0.0, "end": 1.0, "text": "presupuesto"}], True))
    monkeypatch.setattr(finder, "search",
                        lambda segs, q, **kw: [finder.Region(10.0, 70.0, 3, 3, "…presupuesto…")])

    salida = mcp_server.find(str(audio), "presupuesto")

    assert salida["index"] == "caché"
    assert salida["regions"] == [
        {"start": 10.0, "end": 70.0, "hits": 3, "matches": 3, "snippet": "…presupuesto…"}
    ]


def test_voices_lista_el_registro(monkeypatch):
    monkeypatch.setattr(registry, "list_voices",
                        lambda model=None: [{"name": "Voz 1", "model": "pyannote", "seconds": 30}])
    assert mcp_server.voices() == {
        "voices": [{"name": "Voz 1", "model": "pyannote", "seconds": 30}]
    }


def test_probe_devuelve_maquina_y_ruta():
    # conftest fija la máquina: win32, sin GPU, sin whisper.cpp, 32 GB
    salida = mcp_server.probe()
    assert salida["machine"] == {
        "platform": "win32", "cpu_count": 8, "ram_gb": 32.0, "cuda": False,
        "gpu_name": None, "vram_free_gb": None, "whispercpp": None,
    }
    assert salida["route"]["engine"] == "faster-whisper"
    assert salida["route"]["device"] == "cpu"
    assert salida["route"]["compute_type"] == "int8"
    assert "error" not in salida


def test_probe_dice_el_motivo_cuando_el_modelo_no_cabe(monkeypatch):
    from speechtotext.core import probe as core_probe

    chica = core_probe.Machine("linux", 2, 2.0, False, None, None, None)
    monkeypatch.setattr(core_probe, "machine", lambda: chica)

    salida = mcp_server.probe()

    assert salida["route"] is None
    assert "RAM" in salida["error"]
    assert salida["machine"]["ram_gb"] == 2.0


def test_serve_sin_el_sdk_dice_como_instalarlo(monkeypatch):
    monkeypatch.setitem(sys.modules, "mcp", None)   # fuerza ImportError en el import
    with pytest.raises(SystemExit) as ei:
        mcp_server.serve()
    assert "speechtotext[mcp]" in str(ei.value)


def test_serve_registra_las_cuatro_herramientas_y_sirve(monkeypatch):
    registradas = []
    corrido = []

    class ServidorDoble:
        def __init__(self, nombre):
            registradas.append(("nombre", nombre))

        def tool(self):
            def decorador(fn):
                registradas.append(fn.__name__)
                return fn
            return decorador

        def run(self):
            corrido.append(True)

    paquete = ModuleType("mcp")
    submodulo = ModuleType("mcp.server")
    submodulo.MCPServer = ServidorDoble
    paquete.server = submodulo
    monkeypatch.setitem(sys.modules, "mcp", paquete)
    monkeypatch.setitem(sys.modules, "mcp.server", submodulo)

    mcp_server.serve()

    assert registradas[0] == ("nombre", "speechtotext")
    assert registradas[1:] == ["transcribe", "find", "voices", "probe"]
    assert corrido == [True]


def test_el_modulo_no_importa_el_sdk_al_cargarse():
    """Si mcp_server importara `mcp` arriba, `speechtotext --help` exigiría el extra."""
    fuente = Path(mcp_server.__file__).read_text(encoding="utf-8")
    arriba = fuente.split("def serve", 1)[0]
    assert "import mcp" not in arriba and "from mcp" not in arriba
```

- [ ] **Step 2: Correrlos y verlos fallar**

```bash
.venv/Scripts/python.exe -m pytest tests/test_mcp.py -q -p no:cacheprovider
```

Esperado: error de colección — `ModuleNotFoundError: No module named 'speechtotext.cli.mcp_server'`.

- [ ] **Step 3: Escribir el módulo**

Crea `src/speechtotext/cli/mcp_server.py`:

```python
"""Servidor MCP sobre stdio: cuatro herramientas delgadas sobre el núcleo.

Las cuatro son funciones planas con anotaciones (el SDK deriva el esquema de ahí) y no
tocan `mcp`: asi se prueban sin el extra instalado, y `speechtotext --help` no lo exige.
`serve()` es lo unico que lo importa.

Nada aqui imprime. Un servidor stdio que escriba en stdout rompe el protocolo: por eso
`on_progress=None` en vez del callback rich del CLI.
"""
from __future__ import annotations

from pathlib import Path
from types import SimpleNamespace

NOMBRE = "speechtotext"


def transcribe(path: str, language: str = "auto", model: str = "large-v3",
               diarize: bool = False) -> dict:
    """Transcribe un audio o video local y escribe el JSON junto al archivo."""
    from speechtotext.core.formats import write_json
    from speechtotext.core import transcribe as nucleo

    audio = Path(path)
    t = nucleo.transcribe(audio, model=model, language=language, diarize=diarize,
                          on_progress=None)
    destino = audio.with_suffix(".json")   # el mismo default que el CLI sin -o
    info = SimpleNamespace(language=t.language,
                           language_probability=t.language_probability,
                           duration=t.duration)
    write_json(t.segments, info, destino, engine_info=t.engine.to_dict(),
               speech_s=t.speech_s, gaps=t.gaps)
    return {
        "text": "\n".join(s.text.strip() for s in t.segments),
        "json": str(destino),
        "language": t.language,
        "duration": t.duration,
        "warnings": list(t.warnings),
    }


def find(path: str, query: str) -> dict:
    """Busca palabras en un audio largo y devuelve las regiones donde aparecen."""
    from speechtotext.core import finder

    segments, cached = finder.load_or_build_index(Path(path), "tiny")
    return {
        "index": "caché" if cached else "construido",
        "regions": [
            {"start": r.start, "end": r.end, "hits": r.hits,
             "matches": r.matches, "snippet": r.snippet}
            for r in finder.search(segments, query)
        ],
    }


def voices() -> dict:
    """Lista las voces registradas para identificar hablantes."""
    from speechtotext.speakers import registry

    return {"voices": registry.list_voices()}


def probe() -> dict:
    """Sondea esta máquina y dice qué ruta elegiría `transcribe` para large-v3."""
    from speechtotext.asr import AsrError
    from speechtotext.core import probe as sondeo

    m = sondeo.machine()
    salida: dict = {"machine": {
        "platform": m.platform, "cpu_count": m.cpu_count, "ram_gb": m.ram_gb,
        "cuda": m.cuda, "gpu_name": m.gpu_name, "vram_free_gb": m.vram_free_gb,
        "whispercpp": str(m.whispercpp) if m.whispercpp is not None else None,
    }}
    try:
        r = sondeo.choose_route(m, "large-v3")
    except AsrError as e:
        # El sondeo nunca cambia el modelo: si no cabe, lo dice y para (spec §5.1).
        salida["route"] = None
        salida["error"] = str(e)
        return salida
    salida["route"] = {"engine": r.engine, "device": r.device,
                       "compute_type": r.compute_type, "reason": r.reason,
                       "eta_factor": r.eta_factor, "estimated": r.estimated}
    return salida


HERRAMIENTAS = (transcribe, find, voices, probe)


def serve() -> None:
    """Registra las cuatro herramientas y sirve por stdio. Bloquea hasta que cierren."""
    try:
        from mcp.server import MCPServer
    except ImportError as e:
        raise SystemExit(
            'El servidor MCP necesita el SDK oficial. Instálalo con:\n'
            '    pip install "speechtotext[mcp]"'
        ) from e

    servidor = MCPServer(NOMBRE)
    for fn in HERRAMIENTAS:
        servidor.tool()(fn)
    servidor.run()     # stdio por defecto
```

- [ ] **Step 4: Correr los tests hasta verde**

```bash
.venv/Scripts/python.exe -m pytest tests/test_mcp.py -q -p no:cacheprovider
```

Esperado: **8 passed**.

Si `test_serve_sin_el_sdk_dice_como_instalarlo` no levanta `SystemExit`: poner `None` en
`sys.modules["mcp"]` hace que `from mcp.server import MCPServer` levante `ImportError` en
CPython; si tu versión lo levanta como otra cosa, amplía el `except` en el módulo y anótalo.
No cambies el test para que pase.

- [ ] **Step 5: El comando del CLI**

En `src/speechtotext/cli/app.py`, **después** del bloque de `models_app` (al final del
archivo), añade:

```python
@app.command()
def mcp() -> None:
    """Sirve las herramientas por MCP sobre stdio (Claude Desktop y compatibles)."""
    from speechtotext.cli import mcp_server

    mcp_server.serve()
```

El import es perezoso por la misma razón que el resto del archivo: `speechtotext --help` no
tiene por qué cargar nada que no vaya a usar.

- [ ] **Step 6: Test del comando**

En `tests/test_cli.py`, al final del archivo, añade:

```python
def test_mcp_llama_a_serve(monkeypatch):
    from speechtotext.cli import mcp_server

    llamado = []
    monkeypatch.setattr(mcp_server, "serve", lambda: llamado.append(True))
    resultado = runner.invoke(app, ["mcp"])
    assert resultado.exit_code == 0, resultado.output
    assert llamado == [True]
```

`runner` es el `CliRunner()` global de ese archivo (`tests/test_cli.py:12`) y `app` ya está
importado ahí: no crees ninguno de los dos.

- [ ] **Step 7: El extra en `pyproject.toml`**

En `[project.optional-dependencies]`, **después** de `dev`, añade:

```toml
mcp = [
    # SDK oficial. La v2 cambio el import (mcp.server.MCPServer, no
    # mcp.server.fastmcp.FastMCP): el tope mayor no es decorativo.
    "mcp>=2.2,<3",
]
```

No toques `dependencies`: el núcleo no gana ninguna.

- [ ] **Step 8: Gate**

```bash
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

Esperado: **476 passed** (467 + 8 + 1).

- [ ] **Step 9: Comprobar a mano que `--help` no exige el extra**

```bash
.venv/Scripts/speechtotext.exe --help
```

Esperado: el listado incluye `mcp` y sale 0, **sin** el SDK instalado (no lo está: el
`pip install` del extra no se corre en esta tarea).

- [ ] **Step 10: Commit**

```bash
git add src/speechtotext/cli/mcp_server.py tests/test_mcp.py src/speechtotext/cli/app.py tests/test_cli.py pyproject.toml
git commit -m "feat(mcp): servidor stdio con transcribe, find, voices y probe"
```
---

### Task 6: documentación, índices y barridos finales

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/api.md`
- Modify: `docs/README.md`
- Modify: `src/speechtotext/cli/app.py` (docstring de cabecera)
- Modify: `.gitignore` (comentario obsoleto)

**Interfaces:**
- Consumes: todo lo que dejaron las Tareas 1-5. En particular `mcp_server.HERRAMIENTAS` y el comando `mcp`.
- Produces: nada. Es la última tarea.

Tarea de documentación y limpieza. Ni una línea de lógica. Pero **sí toca `docs/api.md`**,
así que el test de contrato de la Tarea 2 la vigila: si al añadir el párrafo del MCP se
rompe algún bloque de código del documento, el gate lo dice.

- [ ] **Step 1: `README.md` — los subcomandos y la instalación**

En la tabla `## Los subcomandos`, añade una fila al final:

```markdown
| `mcp` | Servir las herramientas por MCP sobre stdio, para un cliente como Claude Desktop. |
```

En el bloque de `## Instalación`, añade al final del bloque de código:

```bash
# Servidor MCP (SDK oficial `mcp`)
pip install -e ".[mcp]"
```

- [ ] **Step 2: `README.md` — sección del MCP**

Inserta esta sección **justo antes** de `## Salida` (hoy `README.md:378`):

````markdown
## Servidor MCP

`speechtotext mcp` expone cuatro herramientas por stdio, para clientes MCP como Claude
Desktop. Requiere el extra: `pip install -e ".[mcp]"`.

| Herramienta | Qué hace |
|---|---|
| `transcribe(path, language?, model?, diarize?)` | Transcribe y escribe el JSON junto al audio; devuelve el texto y la ruta. |
| `find(path, query)` | Las regiones del audio donde aparece la consulta, sin transcribirlo entero. |
| `voices()` | Las voces registradas. |
| `probe()` | Qué tiene la máquina y qué ruta elegiría `transcribe`. |

Configuración del cliente (ajusta la ruta al ejecutable de tu entorno):

```json
{
  "mcpServers": {
    "speechtotext": {
      "command": "/ruta/a/tu/venv/bin/speechtotext",
      "args": ["mcp"]
    }
  }
}
```

El servidor no imprime nada por su cuenta: en stdio, stdout es el protocolo. Las
transcripciones largas no reportan progreso por esa razón — el cliente espera.
````

- [ ] **Step 3: `README.md` — estructura del paquete y la línea del CI**

En el árbol de `## Estructura del paquete`, reemplaza la última línea:

```
└── cli/app.py            typer: transcribe / find / enroll / voices / forget / bench / probe / models
```

por

```
└── cli/                  la superficie de usuario
    ├── app.py            typer: transcribe / find / enroll / voices / forget / bench / probe / models / mcp
    └── mcp_server.py     las cuatro herramientas MCP y el servidor stdio
```

En `## Desarrollo`, reemplaza

```markdown
El CI de GitHub Actions está deshabilitado: los gates son locales.
```

por

```markdown
El CI corre la suite en Linux, macOS y Windows sobre Python 3.11 y 3.14, y además
construye el wheel y lo instala en un venv limpio para comprobar que el paquete sirve
fuera de este árbol. El gate local sigue siendo `pytest -q`.
```

- [ ] **Step 4: `docs/api.md` — el párrafo del MCP**

Al final de la sección `## Módulos que no son contrato de esta librería`, añade:

```markdown
- **`cli/`** tampoco es contrato, incluido `cli/mcp_server.py`. Las cuatro herramientas
  que sirve `speechtotext mcp` son envoltorios de `core.transcribe`, `core.finder`,
  `speakers.registry` y `core.probe`: el contrato es el de abajo, no el de la envoltura.
  Si quieres llamarlas desde Python, llama a lo envuelto.
```

- [ ] **Step 5: `docs/README.md`**

En la fila de `api.md` de la tabla de ese índice, actualiza la descripción para que
mencione lo que ahora cubre. Reemplaza el texto de la celda derecha por:

```markdown
Contrato para consumidores: esquema del JSON, la capa `audio/` entera, `asr/`, `transcribe()`, sondeo y modelos, registro de voces e identificación. Lo vigila `tests/test_api_contract.py`; lo que se rompa al cambiarlo sale en el `CHANGELOG.md`.
```

- [ ] **Step 6: `CHANGELOG.md`**

Bajo `## Sin publicar`, en la subsección `### Añadido`, añade al final:

```markdown
- `speechtotext mcp`: servidor MCP sobre stdio con cuatro herramientas (`transcribe`,
  `find`, `voices`, `probe`), tras el extra opcional `[mcp]`. El núcleo no gana
  dependencias.
- `docs/api.md` documenta la capa `audio/` entera (20 símbolos de `audio.__all__`, antes
  3) y los tipos de `asr/` que faltaban. `tests/test_api_contract.py` la vigila en las dos
  direcciones: nada se puede exportar sin documentar, ni documentar sin que exista.
```

Y crea, **debajo** de `### Añadido` y antes de `### Cambiado — rompe`, una subsección nueva:

```markdown
### Cambiado

- CI activado de verdad: la suite corre en Linux, macOS y Windows sobre Python 3.11 y
  3.14, y un job aparte construye el wheel y lo instala en un venv limpio. Antes el
  workflow existía pero instalaba un extra (`[evaluation]`) que ya no existe.
- `tests/fixtures/whispercpp_ojf.json` pasa a ser sintética: misma forma de la salida
  `-ojf`, sin habla humana real y sin rutas de máquina.
```

Si `### Cambiado` ya existe en esa sección, añade las viñetas ahí en vez de crearla.

- [ ] **Step 7: El docstring de cabecera del CLI**

En `src/speechtotext/cli/app.py:7`, el ejemplo apunta a un archivo real del árbol que se
purga en el lanzamiento:

```
    speechtotext transcribe src/static/audio.wav
```

cámbialo por

```
    speechtotext transcribe reunion.m4a
```

- [ ] **Step 8: El comentario obsoleto de `.gitignore`**

El paquete `src/speechtotext/models/` se fue en `59730b8`; el comentario sigue explicando
por qué el ancla lo protegía. Reemplaza

```
# Modelos descargados por faster-whisper (caché Hugging Face)
# Anclado a la raíz: src/speechtotext/models/ es un paquete Python versionado
.cache/
/models/
```

por

```
# Modelos descargados por faster-whisper (caché Hugging Face)
# Anclado a la raíz para no barrer un `models/` que apareciera dentro del paquete.
.cache/
/models/
```

- [ ] **Step 9: Barridos**

```bash
git grep -niE 'aurelius|jarvis|klara' -- src tests scripts README.md CHANGELOG.md docs/api.md
```

Esperado: **sin salida**.

```bash
git grep -nI -e 'C:\\Users' -e 'D:\\Desktop' -- src tests README.md CHANGELOG.md docs/api.md .github
```

Esperado: sin salida.

```bash
git grep -n 'src/static/audio.wav' -- src tests README.md docs
```

Esperado: sin salida.

Si alguno devuelve algo, arréglalo en esta tarea y anótalo en el reporte con el archivo
y la línea.

- [ ] **Step 10: Gate**

```bash
.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

Esperado: **476 passed**. El test de contrato tiene que seguir verde después de tocar
`docs/api.md`; si no, el Paso 4 rompió un bloque de código del documento.

- [ ] **Step 11: Commit**

```bash
git add README.md CHANGELOG.md docs/api.md docs/README.md src/speechtotext/cli/app.py .gitignore
git commit -m "docs: el MCP, el CI de verdad y el contrato completo en README, CHANGELOG e índices"
```

---

## Fuera de este plan

Cosas que encontré midiendo el árbol y que **no** toca este plan, con dónde van:

- **Nombres de personas en los tests.** `tests/test_registry.py`, `tests/test_identify.py`,
  `tests/test_formats_speaker.py`, `tests/test_diarization_pure.py`, `tests/test_cli.py`,
  `tests/test_transcribe.py`, `tests/test_segments.py`, `tests/test_native_signals.py`,
  `README.md` y `docs/api.md` usan dos nombres propios reales como hablantes de ejemplo, en
  unas 60 apariciones. Uno es el del autor (que firma el repo, así que no es una fuga) y el
  otro es el de una persona cercana. Cambiarlos es mecánico pero ancho — toca slugs del
  registro de voces y salidas JSON esperadas — y **es una decisión, no una tarea**: hay que
  resolverla antes de §9.5, junto con la tabla de `--replace-text`. No se decide sola dentro
  de un plan de superficies.
- **El badge de CI en el README** (§9.6). La URL del badge lleva dentro el nombre del repo
  y el del paquete, y los dos cambian en §9.1 y §9.6. Ponerlo ahora es escribir una URL que
  ya se sabe equivocada. Va con el rename.
- **`LICENSE`** (§9.6): `pyproject` declara MIT y el archivo no existe. Va con el repo nuevo.
- **Traducción al inglés** (§8), incluidas las secciones que este plan escribe en español.
  Se traduce todo junto o no se traduce.
- **Purga de la historia** (§9.5): `src/static/audio.wav`, `docs/superpowers/**`, la tabla
  de reemplazos. Este plan deja de *referenciar* el wav; borrarlo de la historia es
  `filter-repo`.
- **Medir `eta_factor` de faster-whisper en CUDA** (§10): no hay GPU de ≥ 5 GB aquí.

---

## Self-review

**1. Cobertura del spec.**

| Requisito | Dónde |
|---|---|
| §6.2 MCP: extra `[mcp]`, stdio, cuatro herramientas, `on_progress=None`, ~100 líneas | Tarea 5 |
| §6.3 `api.md` documenta `transcribe()`, `Transcript`, `Progress`, `AsrError` y códigos | ya estaba; lo fija `CONTRATO` en la Tarea 2 |
| §6.3 `AsrBackend`, `Caps`, los dos backends, `TranscriptionRequest`/`Result` y sus tipos | Tarea 1 paso 2(a) (`Cap`, `TranscriptionSegment`, `TranscriptionWord`) + lo que ya estaba |
| §6.3 los símbolos de `audio.__all__` | Tarea 1 paso 1 (17 de los 20; los otros 3 ya estaban) |
| §6.3 `speakers`: registry, `identify.assign_names`, `diarization.diarize` | Tarea 1 paso 2(c) + `CONTRATO` |
| §6.3 `probe.machine`, `probe.choose_route`, `models.*` | ya estaban (plan 2b); los fija `CONTRATO` |
| §6.3 esquema JSON | ya estaba |
| §6.3 lo que cambie en `api.md` sale en el CHANGELOG | Tarea 6 paso 6 |
| §6.4 `__all__` ⊆ `api.md`, con el nombre en el fallo | Tarea 2, `test_todo_lo_exportado_esta_documentado` |
| §6.4 lo documentado existe e importa | Tarea 2, `test_lo_documentado_existe_y_se_importa` (R3: lista curada, no parser) |
| §7 cero habla humana real en fixtures | Tarea 3 |
| §7 CI activado, matriz de tres sistemas | Tarea 4 (R5: 3.11 y 3.14) |
| §7 nada en CI descarga modelos ni toca red | ya se cumple vía `conftest.py`; R4 explica por qué no hay marcador |
| §7 job `package`: wheel, venv limpio, `--help` y `probe` | Tarea 4 |
| §7 gate local `pytest -q` sin GPU, red ni modelos | constante global |

Sin huecos. Lo de §7 sobre "las fixtures que dicen hey Jarvis o usan Aurelius como hotword"
ya se resolvió en el plan 2b (el grep sale vacío hoy).

**2. Placeholders.** Ningún "TBD", ningún "similar a la Tarea N", ningún "añade manejo de
errores". Cada paso con código trae el código. Los dos sitios donde el plan le pide criterio
al implementador — el `except ImportError` de `serve()` si su CPython se comporta distinto, y
el nombre del `CliRunner` en `tests/test_cli.py` — son verificaciones contra el árbol con
instrucción explícita de anotar el resultado, no huecos. (`runner` es el nombre; verificado en
`tests/test_cli.py:12`.)

**3. Consistencia de tipos.** `Region(start, end, hits, matches, snippet)` — los cinco campos,
verificados en `core/finder.py:12-19`; el test de la Tarea 5 y el diccionario del módulo usan
los mismos cinco. `EngineInfo` se construye con cinco posicionales y `selection`/`diarization`
por defecto, igual que en `core/transcribe.py:85-100`. `TranscriptionWord(text, start, end,
confidence)` — no `probability`; verificado en `asr/types.py:80-85`.
`finder.load_or_build_index(audio, scan_model, rebuild=False)` se llama con dos argumentos en
el módulo y el doble del test acepta el tercero con default. `list_voices`, no `voices` (R9).

**4. Conteo de tests.** 435 (hoy) → +31 (Tarea 2) = 466 → +1 (Tarea 3) = 467 → +0 (Tarea 4)
= 467 → +9 (Tarea 5: 8 en `test_mcp.py` y 1 en `test_cli.py`) = **476**. Cada tarea declara su
número esperado; si uno no cuadra, para y averigua por qué antes de commitear.
